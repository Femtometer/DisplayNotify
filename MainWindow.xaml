<Window x:Class="DisplayNotify.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="VisionGuard V1.00" Height="400" Width="600"
        ResizeMode="CanMinimize" Icon="/eye.ico" WindowStartupLocation="CenterScreen"
        Background="#F0F4F8" FontFamily="Microsoft YaHei">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#409EFF" />
        <SolidColorBrush x:Key="PrimaryHover" Color="#66B1FF" />
        <SolidColorBrush x:Key="AccentBrush" Color="#36B5A0" />
        <SolidColorBrush x:Key="BorderBrush" Color="#D0D7DE" />
        <SolidColorBrush x:Key="HeaderBg" Color="#EBF5FF" />
        <SolidColorBrush x:Key="InfoBrush" Color="#333333" />

        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
        </Style>

        <Style x:Key="RefreshButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="13,6" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{StaticResource PrimaryHover}" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter Property="Background" Value="#3A8EE6" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DisplayListStyle" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,0,0,7" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder" Background="#F8FBFF" BorderBrush="#E0E7EF" BorderThickness="1" CornerRadius="5" Padding="12,9">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ItemBorder" Property="Background" Value="#EBF5FF" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel LastChildFill="True">
        <Border DockPanel.Dock="Top" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="18,11">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <Border Width="30" Height="30" Background="#EBF5FF" CornerRadius="5" Margin="0,0,10,0">
                        <TextBlock Text="◉" Foreground="{StaticResource PrimaryBrush}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="护眼卫士" FontSize="17" FontWeight="SemiBold" Foreground="{StaticResource InfoBrush}" />
                        <TextBlock Text="游戏与屏幕尺寸监测" FontSize="11" Foreground="#6B7280" Margin="0,1,0,0" />
                    </StackPanel>
                </StackPanel>
                <Button Grid.Column="1" Content="↻ 立即刷新" Style="{StaticResource RefreshButton}" Click="RefreshButton_Click" VerticalAlignment="Center" />
            </Grid>
        </Border>

        <Border DockPanel.Dock="Bottom" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="15,8">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <Ellipse x:Name="StatusIndicator" Width="9" Height="9" Fill="#6B7280" VerticalAlignment="Center" />
                <TextBlock x:Name="StatusText" Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource InfoBrush}" Text="正在检测显示器…" TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="LastCheckedText" Grid.Column="2" Foreground="#6B7280" FontSize="12" VerticalAlignment="Center" />
            </Grid>
        </Border>

        <Grid Margin="5">
            <Border Style="{StaticResource CardBorder}">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Border Background="{StaticResource HeaderBg}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" CornerRadius="6,6,0,0" Padding="14,9">
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <TextBlock Text="当前显示器" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1" Text="每10秒刷新" FontSize="11" Foreground="#6B7280" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <ListBox x:Name="DisplayList" Grid.Row="1" Style="{StaticResource DisplayListStyle}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <Border Width="31" Height="31" Background="#EBF5FF" CornerRadius="5" VerticalAlignment="Center">
                                        <TextBlock Text="▣" FontSize="17" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource InfoBrush}" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding PhysicalSize}" Margin="0,3,0,0" Foreground="#6B7280" FontSize="11" />
                                    </StackPanel>
                                    <Border Grid.Column="2" Background="{Binding BadgeBackground}" CornerRadius="10" Padding="9,3" Margin="12,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Connection}" Foreground="White" FontSize="11" FontWeight="SemiBold" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>
    </DockPanel>
</Window>